--- a/plt-2.5a/src/figure.c
+++ b/plt-2.5a/src/figure.c
@@ -86,7 +86,7 @@
     FontGroupSelect("f", f->fgName);
     switch (f->type) {
     case ARROW:
-	move(x1, y1);
+	movepl(x1, y1);
 	cont(x0, y0);
 	x = (double)(x1-x0)/xinch;
 	y = (double)(y1-y0)/yinch;
@@ -96,12 +96,12 @@
 	y1 = y0 + scl*(y1-y0);
 	dx = 0.4*scl*y*xinch + 0.5;
 	dy = 0.4*scl*x*yinch + 0.5;
-	move(x1+dx, y1-dy);
+	movepl(x1+dx, y1-dy);
 	cont(x0, y0);
 	cont(x1-dx, y1+dy);
 	break;
     case BOX:
-	move(x0, y0);
+	movepl(x0, y0);
 	cont(x0, y1);
 	cont(x1, y1);
 	cont(x1, y0);
@@ -115,7 +115,7 @@
 	PolyDef(x0, y0, CFILL);
 	break;
     case CONNECT:
-	move(x0, y0);
+	movepl(x0, y0);
 	cont(x1, y1);
 	break;
     }
@@ -199,7 +199,7 @@
 	if (xsize > textWidth) textWidth = xsize;
 	if (l->pltNum == DEFAULT) continue;
 	if (l->pltNum < 0 || l->pltNum >= Plot.nPlts) {
-	    err(YES, "Bad plot number %d for legend", l->pltNum);
+        errpl(YES, "Bad plot number %d for legend", l->pltNum);
 	    continue;
 	}
 	havePlts = YES;
@@ -247,7 +247,7 @@
 	case NORMAL:
 	case NCOLZERO:
 	case LINES:
-	    move(x0, y);
+	    movepl(x0, y);
 	    cont(x1, y);
 	    break;
 	case FILLED:
@@ -276,7 +276,7 @@
 	    else scatterplot(plt, x, y, y, y);
 	    break;
 	default:
-	    err(NO, "No legend for plot type `%c'", plt->pm);
+            errpl(NO, "No legend for plot type `%c'", plt->pm);
 	    }
 	}
 }
